# Function: get_system_bits

Get the processor bits of the system (32 or 64 bits).

```admonish success title=""
This function is safe
```

### Return Value
Returns the processor bits of the system (32 or 64 bits).
