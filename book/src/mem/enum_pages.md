# Function: enum_pages

Enumerates all the pages in the calling process, returning them in a list.

```admonish success title=""
This function is safe
```

### Return Value
On success, it returns <code>[[page](./objects-page.md)]</code>; On failure, it returns `None`.
